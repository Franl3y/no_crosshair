# No crosshair
#### *Simple FiveM resource that removes weapons crosshair (beside sniper rifles) in just 20 lines of code!*

### How to install
- Add the **'no_crosshair'** folder to your resources
- Add `ensure no_crosshair` to your `server.cfg`
